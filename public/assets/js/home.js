$(document).ready(() => {
  const token = localStorage.getItem('token');
  // $.ajax({
  //   beforeSend(req) {
  //     req.setRequestHeader('Authorization', `Bearer ${token.toString()}`);
  //   },
  //   type: 'GET',
  //   url: '/home',
  // }).then(() => {
      
  // });
});
